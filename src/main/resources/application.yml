spring:
  application:
    name: humandelivery

  config:
    import: optional:file:.env[.properties


  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  datasource:
    url: ${DB_URL:jdbc:h2:tcp://localhost/~/humandelivery}
    username: sa
    password:

  sql:
    init:
      mode: always
# 내장 h2 사용 시 (주석처리된 부분은 참고용입니다)
#  datasource:
#    url: ${DB_URL:jdbc:h2:mem:데이터베이스명}
#  h2:
#    console:
#      enabled: true
#      path: /h2-console

#  datasource:
#    driver-class-name: ${DB_DRIVER:org.h2.Driver}

jwt:
  secret:
    access: "sadpofjaosdjfoapsjdfjaksopdjfpjaskopdjfopasjdpofjapsodfjapsodjfpaoksdjf"
    accessTokenValidTime: "3600000"


logging:
  level:
    org:
      hibernate:
        SQL: debug
        orm:
          jdbc:
            bind: trace
      org:
        springframework:
          web:
            socket: DEBUG
          messaging: DEBUG
